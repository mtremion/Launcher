using UnityEngine;
using TMPro;

/// <summary>
/// This class contains mains data(s) for the Canvas Sign In.
/// </summary>
public class CanvasSignIn : MonoBehaviour
{
    #region System
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    #endregion
}
